# Radiance Labs Roadmap

Single source of truth for every **model** and **product** iteration we
ship. The swimlane visualizer at `pm/index.html` reads this file directly.

## Sync rules (read before editing)

- **Bidirectional sync**: if you bump a version in `SETUP.md`, you MUST
  add or update the matching row here. If you add a row here, update the
  relevant `SETUP.md` section so run instructions exist.
- **Versioning**:
  - **Major / minor** (e.g. `Planck 1.1`, `Raum 0.1`) are declared by the
    user manually when starting a new iteration.
  - **Fix versions** (e.g. `Planck 1.1.1`, `Raum 0.0.1`) are for small
    improvements landed within the same minor. Claude may add these
    automatically when doing bug-fix work.
- **Status values**:
  - `open` — planned but not started
  - `in progress` — actively being worked on
  - `done` — shipped (usually with a LinkedIn post)
- **Row format**: one pipe-table row per entry. Do not delete rows; move
  them to `done` and add a new row for the next version. History matters.
- **id**: `<swimlane-id>-<slug>`, kebab-case throughout (e.g.
  `1-planck-1-0`). Dots in version numbers become dashes.

## Swimlanes

| swimlane-id | name | kind | description |
|---|---|---|---|
| 1 | Planck | model | Small SGS language models (~100M params) |
| 2 | Hertz | model | Large SGS language models (~1B+ params) |
| 3 | Helmholtz | model | TBD |
| 5 | Klang | model | Audio synthesis via SGS |
| 6 | Raum | model | Text-to-3D Gaussian-splat bridge |
| 7 | Einstein | model | Frontier model (future) |
| 8 | Prisma | product | TBD |
| 9 | Klang | product | Audio demo / app |
| 10 | Raum | product | Text-to-3D web demo |
| 11 | Satz | product | Text demo: Planck LM + blob retrieval (local web app) |
| 12 | VSPS | model | VSPS tokenization: Visual/Semantic/Physical SGS token representation (shared research line, publication) |

## Entries

| id | name | type | status | date_created | notes |
|---|---|---|---|---|---|
| 1-planck-1-0 | Planck 1.0 | model | done | 2026-04-07 | Foundation 100M LM; baseline |
| 1-planck-1-1 | Planck 1.1 | model | done | 2026-04-14 | Validated blob concept |
| 1-planck-1-2 | Planck 1.2 | model | done | 2026-04-20 | Six-run ablation matrix shipped 2026-04-28; gate FAIL (val loss 2× worse, speedup 1.07×). Diagnoses at results/planck_12/README.md. Remediation tracked as Planck 1.2.1 |
| 1-planck-1-2-1 | Planck 1.2.1 | model | done | 2026-04-28 | Closed 2026-05-01 as FAIL alongside 1.2.2. Two-track accel remediation ran 2026-04-28; 4 of 8 runs crashed (scheduler TypeError, sk OOM, all collapsed). Root-cause bugs fixed in 1.2.2; see 1.2.2 row for the verdict |
| 1-planck-1-2-2 | Planck 1.2.2 | model | done | 2026-04-29 | Closed 2026-05-01 as FAIL. Three-bug fix pass landed (Optimizer subclass, flat index_select, additive tl floor + renorm) but the SGS-native compound still OOM'd (all_fix step 27.1k, all_plus_fix step 44.3k, sk_fix step 10k) because forward [B·H, L, k, d_f] activation is ~8 GiB bf16 × 3 passes and Windows shared-GPU-memory was spilling until system RAM exhausted. Muon regressed +0.48 nats at matched tokens. No gate passed. Decision: shelve the SGS-native accel track at 100M scale; Hertz 1.2 runs on plain AdamW. Learnings (Optimizer subclass, index_select, harness speedup mask) stay in the tree |
| 1-planck-1-3 | Planck 1.3 | model | done | 2026-04-27 | Shipped 2026-05-26: Wikipedia base retrain (1.3.0, PPL 24.96) + static Wikipedia blob index (1.3.1a, 200k blobs, 960 MB). Live RSS blob index (1.3.1b) deferred post-Hertz. Raum 0.1/1.1 use the 1.3.0 base as frozen token encoder. Details at docs/plans/planck_13_plan.md |
| 1-planck-1-4 | Planck 1.4 | model | done | 2026-04-27 | Shipped 2026-05-21: conversation-memory blobs. DynamicBlobStore + TurnEncoder + HybridRetriever + chat harness. Needle benchmark: 40% retrieval recall at 90+ turn distance (similarity-only), blob-memory retrieves facts truncation loses, wall-clock 5.99s vs 3.68s (within 2x). Cost-slope gate invalid at 512-token context (all modes saturate identically). Results at results/planck14_needle_findings.md |
| 1-planck-2-0 | Planck 2.0 | model | in progress | 2026-06-22 | PATH B (new-architecture experiment), ~100M target. **GROUNDING GATE PASSED 2026-07-06 (0.37, honestly-derived).** CLIP-IMAGE gate cleared after 3 bug-fixes: (1) sense phrases were the bare colliding word so SD drew the dominant prior for both senses -> rewrote to concrete phrases (crane/plane/pitcher now separate at V-cos 0.53/0.52/0.43); (2) concat-cosine == mean-of-block-cosines so an identical S block floored separation -> switched to MAX-aggregation (separable if ANY modality distinguishes; matches grey-fur-vs-boulder); (3) P was a hand material table (tautology) -> DERIVED P via the P6 MLP from the sense phrase (derive_p6_from_vector.py). Result: max-agg V,S = 0.355 PASS (carried by auto CLIP-image V); + derived-P = 0.367 (derived-P a small honest bonus, contrasts now modest: crane 0.18 vs old inflated 1.0). Nothing hand-labeled. Corpus DECIDED = filtered Wikipedia (COCO tried+rejected: low polysemy). VSPS tokenizer BUILT (vocab + sense-tagging, phase 1-2). NEXT: SD-ground top-5k Wikipedia polysemous words (overnight), then train (train_planck2.py TO BUILD). Build plan: SETUP_202607_VSP_v1.md. HONEST scope: gate is a 20-word probe; real result is a trained model beating a SentencePiece baseline on disambiguation. Full gate history below. |
| 1-planck-2-0-gate-history | Planck 2.0 gate history | model | done | 2026-06-22 | PATH B (new-architecture experiment), ~100M target. VSP architecture: each SGS object carries Visual + Semantic + Physical components as ONE bundled representation, not three matrices bound after the fact. Disambiguation falls out for free -- "bank (river)" and "bank (institution)" become DISTINCT tokens because their V (water vs building) and P (none vs concrete) differ at identical text, fixing the polysemy collapse that FAILED Raum 1.2 (SentencePiece merged related words, 35 collision groups / 300 classes). Seed V from Objaverse/ShapeNet blob libraries, P from the Physical Gaussians P6 material work (hardness R^2=0.54); two-tier vocab (grounded tokens rich V/S/P, abstract tokens S-only). GATING PROBE -- FOUR REGIMES RUN 2026-06-22..23 (vsp_gating_probe + derive_vsp + ground_vsp + derive_vsp_clip): hand-seeded 0.48 PASS (grades own labels); text-GloVe 0.13 FAIL (text collapses on colliding surface forms crane/plane); synset one-hot 0.41 PASS but TAUTOLOGICAL (different id -> orthogonal); CLIP-TEXT 0.18 FAIL (CLIP text encoder still sees the shared word, dedup even merged crane-machine into bird at sim 1.0). CONCLUSION: every text-based V fails; synset passes only by construction. DECISION 2026-06-23: ground V in GENERATED IMAGES (CLIP-image embedding of SD-generated views) not ShapeNet -- open-vocab (no ~30-cat ceiling) and visually-distinct senses can't collide. Curation AUTOMATES: Gemma enumerates senses -> SD views -> CLIP embed -> embedding-space DEDUP (once a sense region is occupied, next sample must separate). Real gate = CLIP-IMAGE on the box (--v-source clip-image), one-hot tail removed. Train Planck 2.0 ONLY if that passes. Success -> separate publication. Depends on VSPS tokenization (swimlane 12). Runs parallel to Path A. |
| 2-hertz-1-0 | Hertz 1.0 | model | done | 2026-04-07 | Paused 2026-04-20, wall-clock infeasible without accel |
| 2-hertz-1-2 | Hertz 1.2 | model | done | 2026-04-20 | DONE 2026-07-01: 640M SGS LM (d_f=3700), 1 epoch over 9.69B tokens (FineWeb-Edu + 30% Wikipedia), plain AdamW, ~5 days on RTX 4090. Final best.pt val loss 3.1832 / ppl 24.1. Base COMPLETION model (not instruction-tuned; prompt with sentence starts). Resume bug found+fixed (project_hertz_resume_epoch_restart: --resume replayed the whole epoch while LR carried forward -> cosine LR rose on re-seen data; hard opt-step-budget break added, 125e5f1). Watch-outs (project_sgs_hertz_maxtokens_trap): --max-tokens does NOT stop the loop, stale data dir, --keep-last rotates. Details in SETUP_hertz_202606_1.md |
| 5-klang-1-0 | Klang 1.0 | model | done | 2026-04-10 | Initial audio-SGS concept + scene.py scaffold |
| 5-klang-1-1 | Klang 1.1 | model | done | 2026-04-15 | Variants A & B trained; phase warble + sub-200Hz dropout findings |
| 5-klang-1-2 | Klang 1.2 | model | done | 2026-04-20 | Complex-valued Gaussians, transmittance compositing, MRSTFT. Shipped 2026-04-28: Gate A (MSE 0.00156) + Gate B (log-MAE 1.378 vs Variant B 2.057) both pass. Bass dropout + Nyquist whine fixed. Still behind Klang 1.1 Variant A (stft_3000g) on absolute quality → 1.3 |
| 5-klang-1-3 | Klang 1.3 | model | done | 2026-04-28 | CLOSED 2026-06-22 (not pursuing). Klang route (model swimlane 5 + product swimlane 9) parked to focus on the Raum/Planck/Hertz architecture work (Path A v1 + Path B Planck 2.0 + VSPS tokenization). The 1000/2000/3000g sweep to beat Klang 1.1 Variant A is not being run. Klang 1.2 stands as the last shipped audio-SGS result (Gate A+B passed). Reopen if audio-SGS returns to focus. Details in SETUP_202605.md §6 |
| 6-raum-1-0 | Raum 1.0 | model | done | 2026-04-24 | Template-routing bridge; 6-object hexad, 2-object scenes, analytic-label training. Shipped 2026-04-27 as backbone of Raum 0.0 demo |
| 6-raum-1-1 | Raum 1.1 | model | done | 2026-04-27 | Shipped 2026-05-08: frozen Planck 1.3 encoder + 5.15M bridge (d_model=256/6L/8H) + Objaverse 300-class blob library + relation head + DSL v1 + editable demo. 100% accuracy on procedural scenes. Details at docs/plans/raum_11_plan.md |
| 6-raum-1-2 | Raum 1.2 | model | done | 2026-05-09 | PARTIAL. Trained (50k samples, 100 epochs) but blob accuracy 0.1%. Root cause: SentencePiece subword tokenizer collapses related words (35 collision groups across 300 classes). Raw embedding lookup from a subword LM is architecturally wrong for word-level discrimination. Demo ships with GloVe path. Findings documented in SETUP_202605.md §4.5 |
| 6-raum-1-3 | Raum 1.3 | model | done | 2026-05-18 | Shipped 2026-05-21: recursive semantic-to-geometric decomposition. Trained decomposer on 100+ synthetic trees, inference pipeline generates composition trees from arbitrary prompts, procedural Gaussian fill, interactive web UI. 55-60 Gaussians per scene at depth 1. Details at docs/plans/raum_13_plan.md |
| 10-raum-0-0 | Raum 0.0 | product | done | 2026-04-26 | Local web demo; polish + shader fix shipped 2026-04-27. Known limits (3+ objects, chained relations) deferred to 0.1 |
| 10-raum-0-1 | Raum 0.1 | product | done | 2026-04-27 | Shipped 2026-05-19: GloVe + 300-class Objaverse blob library + 5-object scenes + relation head + editable DSL v1. Colors, spatial relations, and object identity all functional. Known limits: multi-word relations, OOV ambiguity ("plane" -> airplane). These motivate Raum 1.3 recursive decomposition. |
| 10-raum-0-3 | Raum 0.3 | product | done | 2026-05-18 | Shipped 2026-05-21: interactive web demo of Raum 1.3 recursive decomposition. Prompt input, "Decompose + Render" button, live composition tree JSON, Three.js 3D viewer with orbit controls. Arbitrary text-to-3D via trained Planck decomposer. |
| 6-raum-1-4 | Raum 1.4 | model | done | 2026-05-26 | Shipped 2026-05-27: high-fidelity scene generation. Template subdivision (60->13K Gaussians) + gradient densification (13K->51K) + SGS-native refinement (per-cluster Chamfer matching to 109 Sketchfab architecture scans). Beats external multiview consistency on surface coverage (+21%), silhouette fill (+5%), compactness. Export pipeline: .ply, .splat, .obj. 43 unit tests. Details at docs/plans/raum_14_implementation.md |
| 10-raum-0-4 | Raum 0.4 | product | done | 2026-05-27 | SUPERSEDED (closed 2026-06-22): high-fidelity product demo (quality toggle, refinement-mode selector, .ply/.splat export) landed and was overtaken by 0.5 (honest castle), 0.6 (realism), 0.7 (gsplat renderer + densify). The SGS-native refinement path it exposed was later defaulted OFF (distorted clean snapped geometry) and the external-scan route killed by 1.7's analysis. Demo capabilities live on in the 0.5-0.7 viewer. Details at SETUP_202606.md §3 |
| 6-raum-2-0 | Raum 2.0 | model | open | 2026-05-27 | Physical Gaussians: extend SGS primitive with material embedding (e_p). Two-stage architecture: discrete classifier + lookup (current), continuous prediction (Hertz scale). P6 correlation validated at small scale (hardness R^2=0.54). 6/7 Lean 4 proofs complete (Aristotle). Whitepaper + literature review + formal math spec at docs/papers/physical_gaussians*.md |
| 10-raum-0-5 | Raum 0.5 | product | done | 2026-06-01 | Shipped 2026-06-01: honest castle on a hill. Deterministic scene grammar (atomic parts: stone/crenellation/tower/gate/keep/hill/tree, corner-tower + centered-gate spatial rules) renders a recognizable low-fidelity castle with NO model, via --scene-file in the demo (prebuilt=True skips subdivision; ~2.6K parts -> ~51K splats). GATE MET: visibly a castle on a hill (4 cream towers w/ red roofs, crenellated walls, keep, green dome), no blobs/sphere dust. Proves the fill->densify->refine->render stack hits the target and is the 1.5 training-data generator. Details at SETUP_202606_2.md §3 |
| 6-raum-1-5 | Raum 1.5 | model | done | 2026-06-01 | Shipped 2026-06-02: decomposer retrained on the 0.5 grammar's SHALLOW skeletons (parts as leaves; fill stage re-expands tower->body+cren+roof via shared builders, reconciling the model path with the 0.5 render). 21.6K records, max ~440 tok (model context is 512, not 768). GATE MET on the headline prompt: "a castle on a hill" generates a recognizable castle FROM A SENTENCE (towers, crenellated walls, red roofs, green hill). Known gaps -> 1.6: accuracy/prompt-faithfulness (collapses onto the dominant template; "a wall with a gate" -> whole castle, "a tower with a gate" -> no gate + unrequested trees) and realism (flat round-disc splats). Details at SETUP_202606_2.md §4 |
| 6-raum-1-6 | Raum 1.6 | model | done | 2026-06-03 | Shipped 2026-06-03: compositional accuracy. Decomposer retrained (Planck 1.3 frozen) on the SceneSpec compositional grammar; val_loss 0.107. PROMPT-FAITHFUL: "tower on a hill" -> tower, "a wall and a gate" -> wall not castle, counts honored. Greedy decoding + parse-retry killed the flaky JSON failures; structural JSON recovery as net. Grammar-validated decoding drops hallucinated leaves; kind-based layout snap caps towers at 4 on the ring. Details at SETUP_20260603.md §3-4 |
| 10-raum-0-6 | Raum 0.6 | product | done | 2026-06-03 | CLOSED 2026-06-22. Realism. DONE: §5.1 oriented-ellipsoid lit splats (InstancedMesh, per-instance quaternion+scale+color, Lambert + sun/hemi/ambient) replacing flat discs; §5.3 windows/arrow-slits carved into wall faces; §5.4 lighting + fake AO; refine defaulted OFF (SGS Chamfer-to-template distorted clean snapped geometry); tower proportions + hill-vs-footprint sizing hand-tuned. Its one OUTSTANDING item, §5.2 real-scan part routing, was KILLED by 1.7's analysis (routing to the 109 low-res ruin scans proved worse than procedural) and the realism payoff instead landed via 0.7 (gsplat renderer + densify/flatten material) + 1.7 (learned geometry). No open work remains. Details at SETUP_20260603.md §5 |
| 6-raum-1-7 | Raum 1.7 | model | done | 2026-06-09 | GATE PASSED 2026-06-09: the decomposer emits a COHERENT castle (4 towers on a ring + 4 walls + keep) with snap_layout DISABLED -- learned WHERE/HOW-BIG end to end, no _CASTLE_LAYOUT magic constants. Stage 1 (sds_refine.py): SDS-through-differentiable-splat-renderer is a usable signal. Stage 2 (optimize_layout.py): pure-numpy ES learns the ~5 PART proportions render-scored, no constants (wall_courses bounded 5-8 after a collapse). Stage 3 (build_stage3_dataset.py + train_decomposer.py + --no-snap): fine-tuned the 1.6 decomposer on the Stage-2 layout (+ non-castle mix for anti-forgetting + layout variants); model emits the full castle for "a stone castle on a green hill". KEY LESSON: prompt-sensitivity, not capacity -- terse "a castle on a hill" under-recalled (1 tower) while the trained phrasing emitted all 9 parts; diagnose_emission.py (counts raw parts pre-snap) was decisive in not wrongly declaring failure from one bad prompt. Polish CONFIRMED FIXED 2026-06-09 retrain: terse-prompt recall (weighted paraphrase 3x -> emits full castle), green hill color (color preserved in skeleton). Remaining: trees not in layout builder (scope gap, not blocker). Learned geometry via render-scored objective (D+B fused) -- the real break from the hand-tuned ceiling. 1.6/0.6 proved the model is reliable about WHAT but we OVERRIDE its WHERE/HOW-BIG with snapping + magic constants (ring corners, scale 1.0, castle y, wall rotations). 1.7 makes the model emit positions AND scales (D) and trains them with a DIFFERENTIABLE render scored against a diffusion/text-image prior (B, the refine_sds stub), so proportions are learned-to-look-right end to end and the snapping scaffolding falls away. Explicitly NOT the path: (a) D trained on the grammar's own constants = just moving magic numbers into weights (circular, skip); (b) routing to the 109 low-res ruin scans = proved worse than procedural in 0.6. D pays off ONLY coupled to a non-circular signal (render objective, or proportions measured off GOOD assets). Needs: differentiable splat renderer + diffusion prior. Full plan in SETUP_202606_04.md (3-axis realism breakdown + D+B staging); origin in SETUP_20260603 "Limit reached" |
| 10-raum-0-7 | Raum 0.7 | product | done | 2026-06-08 | GATE PASSED 2026-06-08. Cheap appearance wins, no new model. DONE: 0.7.1 weathered-masonry material; 0.7.2 real gsplat renderer at /splat (@mkkellogg/gaussian-splats-3d via CDN, verified in browser); 0.7.3 fixed the sunk castle + sunk trees (seat = sphere-surface height at corner-tower radius, not flat factor); densify+flatten-to-surface post-process (scripts/densify_flatten.py) with 4 UI sliders (total splats / density / flatness / weathering) wired through /decompose. Key learnings: gsplat haze was opacity-modelling, not the renderer -- a stone castle is SOLID, keep clones near-opaque (front splat wins) and jitter clones in 3D (tangent-only collapsed walls to a coplanar line). Uniform global knobs only -- no per-part tuning (that ceiling -> 1.7). Plan in SETUP_202606_04.md §0.7 |
| 10-raum-0-8 | Raum 0.8 | product | in progress | 2026-06-22 | PATH A (current-architecture v1) product. **WALKED END-TO-END 2026-07-06 (project_sgs_path1_outcome): grammar fill SHIPS; scaling gave no uplift.** Hertz-as-interpreter works (§1) but the Hertz decomposer is PARITY with Planck (castle decomposition is a saturated ~14-part task, no capacity win). Learned FILL built + plumbed but FAILS the gate: CAPACITY-limited, not recipe -- Chamfer collapse -> bidirectional (right layout, 13 clusters) -> Hungarian one-to-one (correct fix) all rendered blobs; loss flat by epoch 15. The d_s=128/512-slot FillModel can't emit 271-504 structured stones. Production fill = grammar (correct, deterministic). Open items logged as a separate project (bigger FillModel + cosine LR, Stage B SDS render term train_fill.py:~203, Gemma novel parts). LESSON: base-scaling AND learned fill both saturate on a fixed task; representation is the lever -> VSP. Below = original scope. |
| 10-raum-gemma-decomposer | Raum Gemma decomposer | product | in progress | 2026-07-22 | PATH A hotswap extension. Stand up Gemma 4 E4B (google/gemma-4-E4B-it, ~8B, HF AutoModelForCausalLM) as a Raum DECOMPOSER and make it selectable alongside Planck/Hertz. Motivation: path1 (Hertz decomposer = Planck PARITY, task saturated) + Planck 2.0 VSP LOST phase-4 by 3.8pts (project_sgs_vsp_gate) both say small custom bases are the ceiling on this fixed low-entropy task; Gemma is the opposite lever (large pretrained base that already knows JSON). Decomposer output is STRUCTURE-ONLY tree (CompositionNode: name/position/scale/rot/color/children, NO gaussians) so fill+render are untouched -- Gemma only emits the tree JSON, which generate_trees_gemma.py already proves it can. Gate-and-kill: (0) FEW-SHOT zero-training decomposer **PASSED 2026-07-27: 100% JSON-valid / 97.9% part-vocab on 20 prompts, zero training** (scripts/gemma_decomposer.py; only soft-spot = terse prompts under-recall a monolithic "castle"/"fort" leaf, the Raum 1.7 lesson, fixed by enriching the decomposer UI default prompt to "a stone castle on a green hill"). (1) LoRA fine-tune SKIPPED (Gate 0 passed; kept as fallback, train_decomposer_gemma.py TO BUILD only if the recall soft-spot resists few-shot exemplars). (2) HF backend **DONE 2026-07-27**: infer_decomposer.py --backend hf + --adapter, GemmaDecomposer signature-compatible w/ SGS Decomposer (.generate_tree/.last_raw/.scan_library), parse-failure debug guarded by backend. Gemma is now a launch-time-selectable Raum decomposer next to Planck/Hertz. (2.a) FIX 2026-07-27: GemmaDecomposer was returning a bare skeleton -> empty render ("Mean of empty slice"); extracted fill_gaussians/shift_above_ground to module level + GemmaDecomposer._fill runs them (verified ~994 gaussians). (2.b Gate 2) tree-diff harness compare_decomposers.py BUILT (name-Jaccard + pose delta on breadth_prompts.txt) -- because the RENDER can't show a model diff (every splat is grammar/expand_part; two backends emitting the same part list render identically, and castle decomposition is a saturated task so lists converge = path1). KEY 2026-07-27: "ship on wave" fails for ALL backends -- grammar vocab is castle-only (13 parts, no ship/wave); "fill richness caps usable data richness" (path1), grammar expansion helps every backend equally, doesn't showcase Gemma. (5) MULTIMODAL image->tree spike gemma_image_to_tree.py **PASSED 2026-07-27** (Neuschwanstein photo, 3-shot -> valid 10-part tree: cliff+4 towers+wall+4 trees, faithful to the image: chose cliff over exemplars' hill + trees at forested perimeter = real image grounding not template recall; image reach verified via pixel_values + AutoModelForImageTextToText). This is the REAL Gemma differentiator (image input Planck/Hertz structurally CANNOT take; grounds at inference, distinct from dead VSP-for-LM). (6) WIRED INTO RAUM APP 2026-07-27 (commit ef2f308): GemmaMMDecomposer = ONE multimodal load serving both text + image (8B won't fit twice on a 4090); DecomposerManager = Planck/Hertz/Gemma registry, lazy-load, evict-on-switch; /models + /switch + /decompose_image endpoints; UI pass = model-selector dropdown + Text/Image input tabs (Image enabled only for multimodal models) + drag-drop image dropzone + restyled sidebar. Needs python-multipart. NEXT on box: RUN the app (Gate 6: selector lists 3, Gemma enables Image tab, drop castle photo -> render, switch back to Hertz/Planck clean). HONEST scope: text decomposer = cheaper baseline on saturated castle task NOT a better SGS model; multimodal image->tree is the genuine NEW capability now in the product. Plan: SETUP_072026_gemma.md. |
| 10-raum-0-8-orig | Raum 0.8 scope | product | done | 2026-06-22 | PATH A (current-architecture v1) product. Scale the working text->tree->fill->render stack to Hertz sizes and make the output manipulable + integrable/exportable. Three model upgrades feed it: (1) Hertz as the prompt INTERPRETER (replaces Planck 1.3 as the decomposer base), (2) decomposer scaled to Hertz, (3) FILL stage made LEARNED not procedural -- a conditional part->Gaussian generator (part-token + pose -> N splats) trained on grammar+Gemma-4-generated (part -> Gaussian-cloud) pairs, render-score-supervised via the sds_refine.py differentiable path (NOT template Chamfer, which 0.6 proved distorts clean geometry). Gemma 4 (deepmind.google/models/gemma/gemma-4) generates new grammar + training data on the cheap; fill richness caps usable data richness, so generate only what the learned fill can render-supervise. Product layer: additional ways to manipulate Raum output (beyond the current density/flatness/weathering sliders) + clean export so it is integrable with the rest of the stack, AND model/method HOTSWAP -- the viewer lets you select the prompt INTERPRETER (Planck 1.3 vs Hertz vs Planck 2.0), the DECOMPOSER, and the FILL method (grammar vs learned) independently, so Path A and Path B can be compared live in one demo. Hertz also powers Satz under this path. Sequencing: runs in parallel with Path B (Planck 2.0) and shares VSPS tokenization. Plan: SETUP_202607_path1.md. |
| 11-satz-0-1 | Satz 0.1 | product | open | 2026-05-01 | Local web demo of Planck LM + blob retrieval. Prompt textbox, streamed generation, right-panel retrieved-blob list with transmittance weight bars, k-slider. No new training. Updated 2026-05-04: primary path is Planck 1.3 + Wikipedia blobs (after §2 ships); Planck 1.1 + TinyStories is a flagged-placeholder fallback. Runs AFTER Planck 1.3 + Raum 0.1/1.1. Updated 2026-06-22: build as a SIMPLE chat web interface with MODEL SELECTION (pick which model to chat with: Planck 1.3, Hertz, later Planck 2.0), mirroring Raum's hotswap so both products expose the same model menu. Klang no longer a dependency (parked). Details at docs/plans/satz_01_plan.md. |
| 2-hertz-blob-index-unified-vs-dual | Hertz blob index: unified vs dual | model | open | 2026-06-12 | POST-HERTZ SPIKE (do NOT contaminate the Hertz 1.2 base run; that run is locked text-only, blobs deferred and built post-pretrain per SETUP_hertz_202606_1.md). Question: once Hertz is trained and semantic (Wikipedia/text) blobs are built, should the blob store be ONE unified index or TWO parallel indices (semantic + visual) with a gating retriever? Visual blobs (build_blobs_shapenet.py: {means, scales_log, opacities, colors} in 3D world coords + RGB) live in a different space than semantic blobs (blob_store.py: Gaussians in the d_s meaning space, retrieved by kernel distance, two-pass alpha-composited with word tokens) -- they are NOT token-aligned and cannot be dropped into the text two-pass render as-is. This is essentially the Physical Gaussians / Raum 2.0 thesis (shared semantic+visual retrieval) and needs paired text<->3D data + a bridge, so it is Raum's problem, not the base LM's. Sequence: (1) text-only Hertz, (2) semantic blobs post-pretrain + Wikipedia-blob QA, (3) THEN evaluate projecting visual blobs into a shared retrieval space against the frozen Hertz encoder. Origin: 2026-06-12 pre-launch discussion. Related: 6-raum-2-0, 1-planck-2-0, 12-vsps-0-1. |
| 12-vsps-0-1 | VSPS 0.1 | model | in progress | 2026-06-22 | Shared research line (both paths), own publication. **TOKENIZER BUILT 2026-07-06** (SETUP_202607_VSP_v1.md phases 1-2): build_vsps_vocab.py mints one grounded token per SENSE (V=CLIP-image, P=derived-P6, S=GloVe) + S-only abstract tier (crane-bird/crane-machine are two tokens, distinct V cos 0.56, identical S cos 1.0). tokenize_vsps.py does WSD at encode time (embedding-Lesk: context vs sense-term) -> 5/5 sense-correct on minimal pairs (crane/bank/plane) where SentencePiece scores 0 by construction. Corpus = filtered Wikipedia; senses come FREE from disambiguation pages (Crane -> bird/machine/surname, no Gemma needed), 130k polysemous words full-dump. Grounding capped by frequency (--limit-words) + non-visual-sense filter (drop surname/album/film). NEXT: SD-ground top-5k, then Planck 2.0. Below = original direction. |
| 12-vsps-0-1-orig | VSPS 0.1 direction | model | done | 2026-06-22 | Shared research line (both paths), own publication. VSPS = Visual/Semantic/Physical SGS tokenization: redesign the tokenizer so a token is a RICH grounded unit, not a compression-optimal subword. The current SentencePiece subword tokenizer is provably wrong for SGS -- it collapses related/polysemous words (FAILED Raum 1.2: 35 collision groups / 300 classes; "plane"=airplane vs flat surface merged). Direction: word/concept-level vocabulary for grounded nouns (each carries V/S/P, so polysemous senses become distinct tokens), subword only for connective tissue; two-tier (grounded vs abstract S-only). This is the representation Planck 2.0 (Path B) is built on AND a drop-in improvement for Path A's interpreter/decomposer, hence shared. Relates to the binding question: if tokens are V/S/P bundles from birth, there is NO separate binding step -- similarity is computed over the full bundle, so disambiguation is free. Open question: where V and P come from at token-MINT time (seed from Objaverse/ShapeNet + Physical Gaussians P6). Related: 1-planck-2-0, 6-raum-2-0. |
